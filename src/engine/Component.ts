// Component.ts
// Marker interface for components. Components are pure data, no logic.

export interface Component {
  // Extend this for specific data types (e.g., PositionComponent, HealthComponent)
}
